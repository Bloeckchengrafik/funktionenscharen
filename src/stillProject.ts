import {makeProject} from '@motion-canvas/core';

import still_main from './scenes/still_main?scene';

export default makeProject({
  scenes: [still_main],
  variables: {
    
  },
  name: 'Funktionenscharen <Standbilder>',
});
